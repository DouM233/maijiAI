根据访谈结果与你提供的视觉参考图，我已经完成了该项目的逻辑闭环。这份需求规约专为“AI 辅助编程”场景优化，逻辑高度结构化，以便你直接输入给 Cursor 或 Windsurf 等 AI 编程工具。

---

## 最终需求规约：企业内部 AI 资产集成管理平台

### 1. 项目概览 (Project Overview)
* **项目目标**：构建一个面向公司内部的 AI 聚合门户，通过统一接口调用多模型 API，实现成本可控、业务沉淀、场景降权的智能化协同办公。
* **核心价值**：降低员工使用 AI 门槛（免翻墙、免账号管理），通过“专家卡片”封装业务 SOP，实现公司知识资产的向量化沉淀与合规审计。
* **技术路线**：由设计师通过 AI 编程工具完成，前端对标 Gemini UI 风格。

---

### 2. 业务逻辑流程 (Business Logic)

#### 2.1 用户使用流
1.  **登录与鉴权**：用户通过公司账号登录，系统根据所属部门加载对应的“专家卡片”。
2.  **通用对话状态**：默认进入类似 Gemini 的极简对话界面，用户可自由选择“快速/思考/Pro”三种模式的模型。
3.  **专家召唤流**：用户点击特定“专家卡片”（如：电商策划专家）：
    * **上下文处理**：系统自动总结当前对话摘要。
    * **确认中间层**：用户预览并编辑摘要，确认后发送给专家。
    * **强绑定激活**：系统自动切换至该专家预设的底层模型（如 Claude 3.5）并注入 System Prompt。
4.  **额度监控**：每轮对话实时计算 Token 消耗，若超过每日配额则触发阻断。

#### 2.2 管理员管控流
1.  **专家上架**：配置专家名称、图标、绑定的底层模型、核心提示词（Prompt）。
2.  **知识挂载**：支持为特定专家上传 PDF/Docx 文件，系统完成向量化（RAG），专家回答时优先检索该文档。
3.  **审计回溯**：管理员可查阅全员对话历史，分析业务复盘数据与 Token 分布。

---

### 3. 详细功能清单 (Functional Requirements)

| 模块 | 功能点 | 详细描述 |
| :--- | :--- | :--- |
| **前端 UI (类 Gemini)** | **中央对话框** | 居中输入框，支持多行文本，支持模型切换下拉菜单。 |
| | **场景卡片库** | 位于输入框下方，按部门（管理、电商、设计）分类排列的功能卡片。 |
| | **左侧导航栏** | 包含“新对话”按钮、历史记录列表、个人设置/额度查询。 |
| | **摘要确认窗** | 召唤专家时弹出的中间态弹窗，支持文本二次编辑。 |
| **后端管理 (Admin)** | **用户/部门管理** | 支持添加员工，设置其所属部门及每日 API 额度（Token/次数）。 |
| | **专家卡片 CMS** | 配置专家绑定的模型类型、Prompt、以及关联的向量库权限。 |
| | **知识库 (RAG)** | 支持文档上传、解析、版本更新，建立专家与文档的映射。 |
| | **审计系统** | 完整的对话日志存储，支持按用户、时间、专家类型检索对话详情。 |
| **核心逻辑** | **API 网关** | 统一管理 Claude/GPT/Gemini 的 API Key，实现后端路由分发。 |
| | **摘要生成算法** | 切换专家前，自动调用轻量模型（Haiku/Flash）对前文进行压缩总结。 |

---

### 4. 约束与边界 (Constraints & Boundaries)

* **模型强绑定限制**：专家卡片模式下，前端模型切换器应处于锁定状态，不可由用户更改，以确保回复质量。
* **成本熔断机制**：当用户当日 Token 消耗达到 100% 时，系统需锁定对话框并提示“今日配额已用完”。
* **安全合规**：所有对话内容在后台数据库中明文保存（仅限审计员可见），需在登录页进行“合规使用告知”。
* **开发约束**：考虑到开发者为“非代码背景”，建议采用 **Next.js + Supabase + Vercel AI SDK** 的技术栈，这是目前 AI 辅助编程最容易生成、部署且维护成本最低的架构。

---

### 5. 初始 Prompt 建议（用于 AI 编程工具）

> “请根据以上需求规约，使用 Next.js 和 Tailwind CSS 帮我搭建前端原型。首页需要一个居中的对话框，模仿 Gemini 的简洁感。输入框下方要有分类的专家卡片（管理工具、电商工具），左侧是侧边栏。请先给出整体的项目目录结构和基础 UI 代码。”

